rules_version = '2';
// All client reads/writes are blocked. The Next.js server (firebase-admin
// running on Cloud Run with a service account) bypasses these rules.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
